
{% for project in projects.projects %}
  <div class="bg-light p-4 rounded">
  <h3>  <a href="{{ project.url | url }}" class="">{{ project.name }}
  </a></h3>
  <p>{{ project.description }}</p>
  </div>
  {# <a href="{{ project.url | url }}" class="btn btn-outline-dark bg-dark badge bg-light btn-sm me-2">{{ project.name }}
    <span class="ms-1 badge bg-light">
    {{ project.description }}
    </span>
  </a> #}
{% endfor %}